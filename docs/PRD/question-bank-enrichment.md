# 题库扩充 PRD · Question Bank Enrichment

| | |
|---|---|
| **状态** | Draft（待评审） |
| **创建日期** | 2026-06-02 |
| **负责人** | yaokexiang |
| **触发来源** | "如果要极大丰富题库该怎么办" —— 出卷流已验证可用，但可售题量极少 |
| **本文定位** | 题库扩充策略的事实源。回指 `docs/PRD/exam-paper-system-overhaul.md`（Phase 2/3）、`CLAUDE.md`（零-API 铁律）、`docs/morning-quiz-authoring-and-grading.md`。 |

---

## 1. 现状（实测数据，2026-06-02 生产库）

全库 **1500 题**，但按"能不能自由印进可售卷"分，真实可用量极小：

| sourceType | 数量 | 能否可售出卷 |
|---|---|---|
| `past_paper_reference` | **1110 (74%)** | ❌ 版权真题，**只存元数据、无原文/多无答案**，`restricted_internal` 默认排除 |
| `ai_generated` | 352 (23%) | ⚠️ 取决于是否已审（见下）|
| `original_school` | **48 (3%)** | ✅ 校本原创，版权干净、可售 |

`ai_generated` 里关键的一笔：**`ai_quick_paper` = 291 道**，是"一键 AI 出卷"的副产物，**从未逐题审核 → 默认排除**出出卷池。这是本 PRD 最重要的反面教材：**没有质量门的 AI 量产，只会制造 291 道没人敢用的废题。**

**可售/可出卷（approved + active + 非 ai_quick）实测**：英语 363、雅思 483、物理 28、计科 33、9709 数学 22(含新作 10)、4024 数学 11、4MA1 数学 0。

**结论**：英语/雅思已厚；理科与数学薄；**自有版权的核心资产只有 48 道**。"1500 题"是虚数。

---

## 2. 目标与约束

- **目标**：极大扩充**可售**（版权干净 + 已审 + 可抽题）的题库，支撑多学科、多 component 的出卷，服务 SaaS。
- **约束**：
  1. **零-API（现状）**：运行时不调 LLM；量产 AI 需付费（= overhaul PRD 的 Phase 3 触发点）。
  2. **版权**：不得用真题原文；可售池只收**原创**或**授权可再分发**内容。
  3. **质量**：每道题须正确 + 过结构校验 + 过审计，否则不入可售池。
- **非目标**：把 1110 道版权真题"洗白"成可售内容（做不到，也不该做）。

---

## 3. 核心判断：瓶颈是"质量门"，不是"生成"

报告（Vibe→可售）的灵魂在此：**扩库的瓶颈从来不是能不能生成，而是生成完谁来把关。** 291 道废题就是证据。

> 所以无论用哪条生成源（AI / 老师 / 导入），都先穿过**同一道质量门**才进可售池。**门是第一性的，源是可插拔的。**

---

## 4. 三条生成源（诚实权衡）

| 源 | 能否上"极大"量 | 版权 | 质量风险 | 成本 | 角色 |
|---|---|---|---|---|---|
| **AI/agent 量产** | ✅ 唯一能上量 | 干净(原创) | 高(必须靠门拦) | **flat-fee agents(现成) 或 付费 API** | **主力**（门建好即可开）|
| **老师贡献** | 🟡 随客户复利 | 干净(校方拥有) | 中(同行评审) | 低 | SaaS 护城河 |
| **手工原创(我)** | ❌ 单点瓶颈 | 干净 | 低(逐题核验) | 低(我的 chat 时间) | 打样/补高价值缺口 |
| **授权采购** | ✅ 瞬间上量 | ⚠️ 许可受限 | 低(专业) | 高+许可谈判 | 可选加速 |

---

## 5. 推荐架构：内容质量门（无论用哪条源）

```
源(AI量产 / 老师写 / 人工导入)
  → 自动 critic（evaluator-optimizer：题面/答案/难度/题型自检）
  → groundedness / 答案正确性校验
  → 结构校验器（已有 paper-structure-validator）
  → 去重（同题/近似题）
  → 人工复核队列（teacher/admin approve）
  → 入可售库（approved_internal + 出处/版权标签）
```
这正是报告 Template C(critic) + evaluator-optimizer + groundedness gate 落到**内容生产**上。出处/版权标签全程携带，决定一道题能否进可售池。

## 5.1 Lever A 的具体形态：flat-fee 多-agent 出题–验证流水线

**老板洞察**：用 Cowork **flat-fee agents** 当生成引擎 → **不烧 per-token API、不必先拍 Phase 3 付费板**。这把 Lever A 从"要预算"变成"现在就能做"——但生成只是第一环。仍然全 flat-fee 的完整形态：

```
出题agent → 独立重解agent(对抗,换方法) → 程序化正确性核验(数理:代回/数值/符号)
  → 质量critic(纲目/清晰/难度/题型) → 去重(检索现有库) → 人工抽审approve → 入可售库
```
**整体节流到「人工抽审产能」，不是「生成速度」。**

### 提示词/规则/世界边界（必要）vs 外部强制门（不可省）
> 单靠提示词解决一切，正是报告要纠正的"原型思维"（Anthropic：并非所有 failing eval 都该靠 prompt 解决）。提示词作用在模型自己的推理里，抓不住它"不知道自己犯"的错。

| 想用"规则/提示词"解决的 | 真正生效的外部形态 |
|---|---|
| 输出合法结构 | 校验器**拒绝**（已有 `paper-structure-validator`）|
| 答案正确 | **独立重解 + 程序化核验**；判不准→打回 |
| 不跑题 / 守纲目 | 世界边界 prompt + **覆盖追踪** |
| 不重复 | **对现有库检索去重** |
| 治理 | pending 状态 + **人工 gate** + provenance 标签 + 监控 + kill switch |

### "两-agent（出题+审题）"的三个坑（必须堵）
1. **共享盲点**：同款模型常犯相关错误 → 审题 agent 必须**独立重解(换方法)**，数理再加**非-LLM 程序化核验**。
2. **reviewer 橡皮图章**：LLM-as-judge 偏向"看着对就放过" → 审题 agent **对抗式(找茬/拿不准就 fail)** + **已知错题种子校准** + **人工抽审其判定**。
3. **两 agent 只保单题质量**：去重/覆盖/治理/入库前人工 gate 仍需上面的外部环兜。

---

## 6. 决策日志

| # | 决策 | 备选 | 理由 |
|---|---|---|---|
| Q1 | **先出本 PRD 再定路线** | 直接建管道 / 直接付费量产 | 涉及付费 + 版权 + 质量多重权衡，先对齐 |
| Q2 | **门优先于源** | 先开 AI 火力 | 291 废题证明：无门的量产=制造垃圾 |
| Q3 | 可售池只收原创/授权可再分发 | 把真题洗进来 | 版权红线 |
| Q4 | Lever A 引擎优先用 **flat-fee Cowork agents** | 直接付费运行时 API | 不烧 token、不必先拍 Phase 3 预算；穿同一道门 |
| Q5 | 提示词/边界**必要不充分**；正确性靠外部 oracle(独立重解/程序化) + 对抗式 reviewer + 人工抽审 | 只靠强提示词/世界边界 | 同模型自评盲点重合；可售标准下残留错答案只能外部闭合 |

---

## 7. 分阶段路线图

### Phase A ｜ 建门 + 盘活存量（**纯零-API、立即可做、零新增成本**）
- 建/补齐"内容质量门"流水线（§5）+ 人工复核工作流（产品已有 ReviewService/Review 页，验证并补齐）。
- **用现有 291 道 `ai_quick_paper` 当第一批输入**穿过这道门：合格的复核提升入可售库，不合格的淘汰。→ **不花一分 API 钱就能深挖存量、同时把门验真。**
- 我继续手工补高价值缺口（如各 component 起步批），走同一道门。

### Phase B ｜ Agent 量产（触发条件：Phase A 的门建好即可，**无需付费**）
- 用 **flat-fee Cowork agents** 跑 §5.1 的"出题→对抗重解→程序化核验→critic→去重→人工抽审"流水线，**全部穿过门**才入库。**因走 flat-fee，不必先拍 Phase 3 付费板**——门一建好就能开。
- 付费运行时 API 仅作**可选的吞吐加速器**（量更大时），不是前提。
- 监控：人工抽审拦截率、单位合格题成本（含人工）、覆盖/去重率。

### Phase C ｜ 老师贡献 + 多校共享（SaaS）
- 老师写题→同门审核入库；跨校共享池随客户增长复利，形成护城河。

---

## 8. 度量

- 可售题量 / 每学科·component（目标：每 component 足以出 N 份不重样卷）。
- 门的吞吐 + **人工复核拦截率**（catch rate）+ 误批率。
- 单位"合格入库题"成本（含 API + 人工）。
- 出卷"凑满目标分"成功率（直接受益指标）。

---

## 9. 风险与护栏

- **垃圾入库**（291 废题重演）→ 门优先、源穿门。
- **正确性**（错答案进可售卷）→ critic + 人工 gate + 抽检。
- **版权**（真题/授权内容泄入可售池）→ 出处标签 + 可售池只收原创/可再分发。
- **零-API 被动破**（量产必须付费）→ Phase B 触发条件量化，先把 Phase A 的零成本价值吃满。

---

## 10. Open Questions

1. 运行时 AI 预算 / 量产规模（多少题/月）？
2. 每学科·component 的目标题量？优先哪几科？
3. 老师贡献的激励与审核 SLA？
4. 是否引入授权题库合作（哪家、什么许可）？
