# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
build/
.next/
out/

# Env / secrets
.env
.env.local
.env.*.local
!.env.example

# Logs
*.log
npm-debug.log*
yarn-debug.log*

# IDE
.idea/
.vscode/
*.swp

# OS
.DS_Store
Thumbs.db

# Testing
coverage/
.nyc_output/

# Prisma
apps/api/prisma/migrations/dev.db*
apps/api/prisma/*.db
apps/api/prisma/*.db-journal

# PDFs / generated
generated-pdfs/
uploads/
*.pdf

# Puppeteer cache
.cache/

# Claude Code / agent tooling (per-machine, not project source)
.claude/
.agents/
skills/
skills-lock.json

# Python bytecode
__pycache__/
*.pyc

# TypeScript incremental build cache
tsconfig.tsbuildinfo

# IELTS test fixtures (PDFs are heavy + may be copyrighted)
apps/api/test-fixtures/*.pdf
apps/api/test-fixtures/*.json

# Scratch / OCR working dirs (intermediate PNG / OCR text — too big for git)
.tmp/
apps/api/test.tmp
*.tmp

# DB backups — contain student PII, live off-site in OneDrive; never commit.
backups/
.seiue_sync_v2/
